<div class="d-flex justify-content-between align-items-center mb-4">
  <h1><%= @identity_provider.name %> を編集</h1>
</div>

<div class="card">
  <div class="card-header">
    <%= @identity_provider.provider_type&.upcase %> IdP 設定
  </div>
  <div class="card-body">
    <%= render "form" %>
  </div>
</div>

<div class="mt-4">
  <%= link_to "戻る", admin_identity_providers_path, class: "btn btn-outline-secondary" %>
</div>
